# נאמן ספרים — book-stand-app

אתר קיוסק (RTL, עברית) לדוכן ספרים ללא מפעיל: הלקוחות סורקים ברקוד/מחפשים ספר וקונים
בעצמם. אתר סטטי בלבד — אין build step, אין שרת. `index.html` + `app.js` + `styles.css`
+ `config.js`, בלי framework.

בעל האתר (המשתמש) **לא טכני**. תסביר דברים בעברית פשוטה, בלי ז'רגון, ותעשה משימות
בעצמך במקום להסביר לו איך ללחוץ על כפתורים — הוא מעדיף את זה מפורשות.

## פריסה (deployment)

- **Hosting:** GitHub Pages, בכתובת `https://neeman1948.github.io/book-stand-app/` —
  זה תת-נתיב, אז כל הפניה לקובץ חייבת להיות יחסית (`./file.js`), לא מוחלטת (`/file.js`).
- **מסד נתונים:** Supabase (Postgres + Realtime + Storage). המפתח הציבורי
  (`sb_publishable_...`) יושב גלוי ב-`config.js` בכוונה — זה אתר סטטי, אין דרך להסתיר
  אותו, וזה בסדר כי מדיניות RLS (ראה למטה) מגבילה מה אפשר לעשות איתו.
- **חוק ברזל של קאש:** כל `<script>`/`<link>` ל-`app.js`/`styles.css`/`config.js` ב-
  `index.html` נושא `?v=YYYYMMDD-slug`. **כל עריכה בקובץ הזה מחייבת עדכון המספר הזה
  באותו commit**, אחרת ה-CDN של GitHub Pages ימשיך להגיש גרסה ישנה בלי שגיאה גלויה.
  זו הסיבה השכיחה ביותר לבאגים "לא רואה את השינוי באתר".
- לפני שמדווחים שמשהו "עלה", לוודא עם `curl` על ה-URL החי (בדיקת build status של
  GitHub Pages דרך ה-API לא אמינה, נתקעת/מציגה סטטוס ישן).

## מוסכמות בקוד

- כל טיפוס נתונים (`book`, `order`, `sale`, `invoice`...) עובר דרך `normalizeX()`
  שסובלני גם ל-camelCase (פנים-אפליקטיבי) וגם ל-snake_case (מ-Supabase).
- פונקציות כתיבה (`persistBook` וכו') הן דו-ענפיות: מקומי (`localStorage`, `render()`,
  `return`) מול מרוחק (כתיבה ל-Supabase, `throw` על שגיאה, `refreshRemoteData()` +
  `render()`).
- `crypto.randomUUID()` (עטוף ב-`newId()`) לכל ה-IDs — תואם ל-`gen_random_uuid()`.
- פיצ'רים חדשים שדורשים שינוי סכימה מוגנים בדגל ב-`config.js` (למשל
  `tables.invoices` + `storageBuckets.invoices`) — כותבים את כל הקוד עם הדגל כבוי,
  בודקים שההתנהגות זהה, ורק אז מדליקים. זו דרך החזרה-לאחור (revert = מחיקת השורות).

## RLS ב-Supabase — החלטה מכוונת, אל תשני בלי לחשוב פעמיים

האפליקציה לא מבחינה בין "לקוח" ל"מנהל" מול Supabase — שניהם משתמשים באותו מפתח anon
ציבורי (אין Supabase Auth). לכן: יש מדיניות `select`/`insert`/`update` לתפקיד `anon`
בכל טבלה, אבל **בכוונה אין אף מדיניות `delete`** — כי מחיקה מרוחקת עם מפתח ציבורי
משותף = כל מי שמחזיק את המפתח יכול למחוק הכל. תוצאה: `.delete()` בלי מדיניות מחזיר
`error: null` (אפס שורות הושפעו, בלי שגיאה) — קל מאוד לפספס את זה ולהראות הודעת
הצלחה כוזבת. אם צריך "מחיקה" מרוחקת — להשתמש בדפוס soft-delete (עמודת `is_deleted`,
`update` במקום `delete`, ראו `deleteInvoice`/`restoreInvoice` ב-`app.js`), ולוודא
עם `.select("id")` שהשורה באמת נכתבה. פתרון אמיתי דורש Supabase Auth למנהל — לא נעשה.

## מה קיים כרגע (2026-08-19) ומה תלוי ועומד

- דוח מכירות: מוגבל ל-5000 שורות (היה 200, נחתך בשקט — תוקן), עם התראה גלויה אם
  עדיין נחתך.
- PWA: manifest.json + אייקונים (ספר לבן על רקע טורקיז) קיימים ופעילים, **בלי**
  Service Worker (בכוונה — היה מתנגש עם מוסכמת ה-`?v=`).
- חשבוניות לענן: כל הקוד ב-`app.js` וב-`supabase-schema.sql` מוכן (Storage bucket
  פרטי, קישורים חתומים, soft-delete/ארכיון), **אבל הדגל ב-`config.js` עדיין כבוי**.
  השלב הבא: המשתמש צריך לספק connection string למסד (Project Settings → Database →
  URI ב-Supabase) כדי שהסכימה תרוץ בלי שהוא יצטרך לגעת ב-SQL Editor בעצמו — הוא
  ביקש מפורשות שהמשימות האלה יתבצעו בשבילו, לא על ידו.
- `vendor/pdfjs/` (כ-3MB) לא בשימוש יותר (עברנו ל-CDN) — לא נמחק, אפשר למחוק בהזדמנות.

## סביבת עבודה

Windows, PowerShell + Git Bash זמינים. `node`/`npm`/`git`/`gh`/`claude` מותקנים
(winget + npm רשמי מ-Anthropic — ראו היסטוריית commits, לא הותקן מסקריפט צד-שלישי
לא מאומת). בדיקות אוטומטיות ב-`test.html` (14 בדיקות) — לפתוח בדפדפן ולהריץ ידנית.
