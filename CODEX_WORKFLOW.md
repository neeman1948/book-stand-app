# איך לעבוד עם Codex בפרויקט הזה

## בתחילת שיחה חדשה

שלח לקודקס את המשפט הבא:

```text
אנחנו עובדים על אפליקציית דוכן ספרים. קרא קודם את הקבצים PROJECT_BLUEPRINT.md, CONTEXT_SUMMARY.json, FUNCTION_REFERENCE.md ו-CHANGELOG.md בתוך outputs/book-stand-app, ואז תמשיך מהמשימה שאבקש.
```

אם יש משימה ספציפית, הוסף אותה אחרי המשפט הזה.

## בסוף שינוי מהותי

בקש:

```text
עדכן את PROJECT_BLUEPRINT.md, CONTEXT_SUMMARY.json, FUNCTION_REFERENCE.md ו-CHANGELOG.md לפי מה שעשית עכשיו.
```

כך לא נבזבז זמן בשיחה הבאה על ניחוש מחדש של מבנה הפרויקט.

## איך לבקש שינויים

מומלץ לבקש בצורה הזו:

```text
תשנה רק את החלק של [שם המסך/הפעולה]. לפני עריכה קרא את הקבצים הרלוונטיים, שמור על המבנה הקיים, ובסוף תעדכן Changelog.
```

## מה לא כדאי לבקש באופן קבוע

- לא כדאי לבקש הערה על כל שורה. זה מנפח את הקוד ומקשה לקרוא.
- כן כדאי לבקש הסברים ליד לוגיקה לא ברורה, וקבצי תיעוד שמסבירים את המערכת.
- לא כדאי לפתוח צ'אט חדש בלי לתת את `CONTEXT_SUMMARY.json` או `PROJECT_BLUEPRINT.md`.

## משפט שימושי לעבודה איכותית

```text
עבוד בזהירות: אל תשבור פונקציות קיימות, אל תסיר אפשרויות בלי לעדכן אותי, בצע בדיקת טעינה בדפדפן, ועדכן את CHANGELOG.md בסוף.
```
